For whatever reason the arduino IDE REQUIRES that the .ino filename is in a folder with the same name.

I recommend you use the src.ino file instead. Since the chance they're going to change this behaviour
might probably very slim sadly.